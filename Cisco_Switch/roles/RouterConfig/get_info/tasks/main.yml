---
  - name: Gather interfaces facts
    cisco.ios.ios_facts:
      gather_subset: 
      - interfaces                        # You can replace this by : [config, default, hardware, interfaces, all]
    register: interfaces_
